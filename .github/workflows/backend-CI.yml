name: CI script for backend
permissions:
  checks: write
  pull-requests: write
  contents: write

on:
  pull_request:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - uses: actions/setup-node@v4
        with:
          node-version: 'latest'

      - run: npm i
        working-directory: Backend/

      - run: Lint & Fix using Standard
      name: |
        npx standard --fix Backend/
      
      - name: Check for remainding lint errors
        run: |
          npx standard Backend/
        continue-on-error: false

      - name: Begin running tests
        run: ENV=test npx jest unit --coverage
        working-directory: ./Backend/

      - name: Publish test results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: (!cancelled())
        with:
          files: |
            Backend/coverage/results.xml

      - name: Upload coverage report to codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: Backend/coverage/lcov.info

      - name: Format code using prettier
        uses: creyD/prettier_action@v4.5
        with:
          prettier_options: --write /home/runner/work/ActionsFunkery/ActionsFunkery/Backend/**/*.{js,jsx,ts,tsx}
          file_pattern: "*.js"
